<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sp.app.mapper.SellerMapper">

    <sql id="category-search">
        <choose>
            <when test="searchType == 'true'">
                ( INSTR(subject, #{kwd}) &gt; 0
                OR INSTR(content, #{kwd}) &gt; 0 ) OR
                ( TO_CHAR(reg_date, 'YYYY-MM-DD') = #{kwd}
                OR TO_CHAR(reg_date, 'YYYYMMDD') = #{kwd} )OR
                ( INSTR(category, #{kwd}) &gt; 0 )
            </when>
        </choose>
    </sql>

    <sql id="category-prize-search">
        <choose>
            <when test="categoryType == 'big'">
                p.PTYPE = '경매' AND CB.CBN = #{cbn}
            </when>
            <when test="categoryType == 'small'">
                p.PTYPE = '경매' AND CS.CSC = #{csc}
            </when>
        </choose>
    </sql>

    <insert id="insertPrize" parameterType="com.sp.app.mypage.controller.dto.request.SellerRequest">
        BEGIN
            INSERT INTO PRODUCT(PNUM, PTYPE, PNAME, PCT, PPH, NUM, CSN)
            VALUES (PRODUCT_SEQ.NEXTVAL, #{sellBox}, #{prName}, #{content}, #{thumbnail,jdbcType=VARCHAR}, #{userId}, #{subCategory});

            INSERT INTO PRODUCT_AUCTION(PNUM, LP, PARD, PASD, PAED, PAS, PAT, FP)
            VALUES (PRODUCT_SEQ.CURRVAL, #{prPrice}, SYSDATE, TO_DATE(#{startDate}, 'YYYY-MM-DD HH24:MI:SS'), TO_DATE(#{endDate}, 'YYYY-MM-DD HH24:MI:SS'), '판매전', #{dealType}, 0);
        END;
    </insert>

    <insert id="insertFile" parameterType="com.sp.app.mypage.controller.dto.request.SellerRequest">
        INSERT INTO PRODUCT_PICTURE(PPNUM, PPP,PNUM)
        VALUES (PRODUCT_PICTURE_SEQ.NEXTVAL, #{file}, PRODUCT_SEQ.CURRVAL);
    </insert>

</mapper>